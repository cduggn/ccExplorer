before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - main: ./cmd/ccexplorer
    binary: ccexplorer
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    gobinary: "go"
    goarch:
      - amd64
      - arm64
      - arm

release:
  prerelease: auto

universal_binaries:
  - replace: true

dockers:
  - image_templates: ["cdugga/ccexplorer:{{ .Tag }}"]
    dockerfile: Dockerfile

brews:
  -
    name: cduggn
    homepage: "https://github.com/cduggn/ccExplorer"
    tap:
      owner: cduggn
      name: homebrew-cduggn
    commit_author:
      name: cduggn
      email: duggan.colin@gmail.com

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

